package com.jy.modules.drools.entity;
import java.util.List;

rule "Rule_A002"
	when
	   $list:List();
       $Current:Applicant();
       $DataBase:Applicant(this.cardId==$Current.cardId) from $list;
	then
	  System.out.println("two same objects in list:DataBase custName :" + $DataBase.getCustName());
end